name: tekartik_firebase_storage_flutter
description: Firebase storage for flutter
version: 0.8.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  path:
  tekartik_firebase_storage:
    git:
      url: https://github.com/tekartik/firebase_storage.dart
      path: storage
      ref: dart3a
    version: '>=0.8.0'
  tekartik_firebase_flutter:
    git:
      url: https://github.com/tekartik/firebase_flutter
      path: firebase_flutter
      ref: dart3a
    version: '>=0.6.0'
  firebase_storage: '>=7.0.0'
  tekartik_firebase:
    git:
      url: https://github.com/tekartik/firebase.dart
      ref: dart3a
      path: firebase

dev_dependencies:
  dev_build:
  flutter_test:
    sdk: flutter
  process_run: '>=0.10.2'
  tekartik_lints_flutter:
    git:
      url: https://github.com/tekartik/common_flutter.dart
      ref: dart3a
      path: packages/lints_flutter
    version: '>=0.1.0'

dependency_overrides:
  tekartik_firebase_flutter:
    path: ../firebase_flutter
#  tekartik_firebase_storage:
#    path: ../../firebase_storage.dart/storage
